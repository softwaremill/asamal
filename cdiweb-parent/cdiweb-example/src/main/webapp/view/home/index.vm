#layout('default')

#region('content')
    <h1>Welcome to home!</h1>

    <ul>
        #{foreach}($item in $list)
            <li>$item</li>
        #{end}
    </ul>

    <a href="$tag.link("home", "register")">Register</a>

    #renderPartial("partial")

    #renderPartial("/common/common_partial")
#end